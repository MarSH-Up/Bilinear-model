import React from "react";
import { Box, Typography } from "@mui/material";

export const HowTo: React.FC = () => {
	return (
		<Box>
			<Typography>Tutorial in Progress</Typography>
		</Box>
	);
};

export default HowTo;
